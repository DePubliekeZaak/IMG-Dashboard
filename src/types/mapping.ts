export interface Mapping {

    label: string,
    column: string | string[],
    colour?: string,
    group?: string,
    short?: string,
    outflow?: any,
    duration?: string,
    units? : string
    format? : string
}
